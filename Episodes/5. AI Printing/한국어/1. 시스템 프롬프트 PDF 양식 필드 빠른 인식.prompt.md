## 사용자가 작성할 PDF 양식을 업로드할 때:

### 빠른 결정 경로:
1. 실행: `check_fillable_fields.py` 
2. "작성 가능한 양식 필드 있음" → 아래 빠른 방법 사용
3. "작성 가능한 필드 없음" → FORMS.md의 주석 방법 사용

### Sejda/온라인 PDF 편집기용 빠른 방법:

**필드 추출 (30초):**
```python
from pypdf import PdfReader
reader = PdfReader("form.pdf")
fields = reader.trailer["/Root"]["/AcroForm"]["/Fields"]
names = [f.get_object()["/T"] for f in fields if "/T" in f.get_object()]
```

**양식 작성 (15초):**
```python
from pypdf import PdfReader, PdfWriter
reader = PdfReader("input.pdf")
writer = PdfWriter()
writer.clone_reader_document_root(reader)  # ← Sejda에 중요
writer.update_page_form_field_values(writer.pages[0], field_values)
with open("output.pdf", "wb") as f: writer.write(f)
```

### 일반적인 필드 패턴:
- **Sejda:** Date, Name, Address, CustomerPhone, Description1-N, Qty1-N, Cost1-N, TotalCost1-N
- **Adobe 양식:** 밑줄을 자주 사용 (First_Name, Last_Name)
- **Google Forms PDF:** 일반적으로 설명적인 이름

### 주요 지표:
- Sejda 필드는 객체에 `/Sejda` 항목이 있음
- 여러 줄 필드는 `/Ff: 4096`을 가짐
- 대부분의 양식은 필드에 페이지 0을 사용

### 이 단계들 건너뛰기:
- ❌ Sejda 양식에 `extract_form_field_info.py` 사용하지 마세요 (0개 필드 반환)
- ❌ `append_pages_from_reader()` 사용하지 마세요 (양식 손상)
- ❌ 작성 가능한 양식에 검증 이미지 만들지 마세요
- ✅ pypdf 직접 검사 + clone_reader_document_root 사용하세요

**절약된 시간: PDF당 약 4.5분**