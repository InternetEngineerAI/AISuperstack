OBJECTIVE:
Obligar a la IA a mostrar su salida normalmente en secciones claramente etiquetadas y luego repetir inmediatamente cada sección dentro de su propio bloque de código markdown para fines de copia/exportación.

REQUIREMENTS:

La IA debe mostrar primero todo el contenido exactamente como está estructurado originalmente, utilizando secciones claramente etiquetadas (no tablas).

Cada sección debe permanecer completamente renderizada y legible en formato markdown normal.

Después de que se muestre cada sección renderizada, la IA debe insertar una línea en blanco.

Inmediatamente después de esa línea en blanco, la IA debe abrir un bloque de código con triple backtick.

Dentro de ese bloque de código, la IA debe repetir SOLO el contenido exacto de esa sección específica tal como se muestra arriba.

Cada sección debe tener su propio bloque de código separado inmediatamente después de ella.

Si se generan múltiples secciones, cada sección debe ir seguida de su bloque de código correspondiente en el mismo orden.

No debe aparecer ningún comentario, explicación o resumen adicional dentro de ningún bloque de código.

Los bloques de código deben contener únicamente el texto sin formato de su sección correspondiente.

No debe haber ningún texto después de los últimos tres backticks de cierre.

No cambies el tema, la audiencia ni la intención estratégica del prompt original.

Solo modifica las instrucciones de formato para aplicar este comportamiento de doble renderizado sección por sección.

NO conviertas ningún contenido en tablas.

NO alteres, resumas ni reestructures el contenido de la sección al repetirlo dentro del bloque de código.

Aquí está el prompt original para modificar:

[PASTE ORIGINAL PROMPT HERE]

Devuelve el prompt completamente modificado.
