OBJECTIVE:
Force the AI to render its output normally in clearly labeled sections, and then immediately repeat each section inside its own markdown code block for copy/export purposes.

REQUIREMENTS:

The AI must first display all content exactly as originally structured, using clearly labeled sections (not tables).

Each section must remain fully rendered and readable in normal markdown format.

After each rendered section is displayed, the AI must insert one blank line.

Immediately after that blank line, the AI must open a triple backtick code block.

Inside that code block, the AI must repeat ONLY the exact content of that specific section as shown above.

Each section must have its own separate code block directly following it.

If multiple sections are generated, each section must be followed by its own corresponding code block in the same order.

No additional commentary, explanations, or summaries may appear inside any code block.

The code blocks must contain only the raw text of their corresponding section.

There must be no text after the final closing triple backticks.

Do not change the topic, audience, or strategic intent of the original prompt.

Only modify formatting instructions to enforce this section-by-section dual-render behavior.

Do NOT convert any content to tables.

Do NOT alter, summarize, or restructure the section content when repeating it inside the code block.

Here is the original prompt to modify:

[PASTE ORIGINAL PROMPT HERE]

Return the fully modified prompt.